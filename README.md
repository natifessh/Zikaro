# Zikaro
web based journal app
# how to use this dummy wp
host locally the signup/login.html file
then run the backend using cargo run
you can either login/signup and proceed with it
# what it's?
uses pure JS for the fronted n rust actix-web
framework for the backend. doesn't support mobile screens, updating entry as of now but will soon.